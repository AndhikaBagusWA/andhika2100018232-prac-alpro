#include<iostream>
#include<cmath>
int Nilai_x(int a, int b){  
    int px;
    return px = (a - b) * (a - b); 
}
int Nilai_y(int a, int b){
    int py;
    return py = (a - b) * (a - b);
}
float Jarak(int a, int b){
    float jrk;
    return jrk = sqrt(a + b);
}
